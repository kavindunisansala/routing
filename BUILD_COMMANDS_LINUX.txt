╔═══════════════════════════════════════════════════════════════════════════════╗
║              🚀 READY TO BUILD - COPY & RUN THESE COMMANDS                   ║
╚═══════════════════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════════════
COPY & PASTE THESE COMMANDS ON YOUR LINUX SYSTEM:
═══════════════════════════════════════════════════════════════════════════════

cd ~/Downloads/ns-allinone-3.35/ns-3.35/scratch

# Rename to .inc extension (NS-3 won't try to compile .inc files)
mv wormhole_attack.cc wormhole_attack.inc 2>/dev/null || mv wormhole_attack_impl.cc wormhole_attack.inc

# Update routing.cc to include the renamed file
sed -i 's/#include "wormhole_attack.*\.cc"/#include "wormhole_attack.inc"/' routing.cc
sed -i 's/#include "wormhole_attack_impl\.cc"/#include "wormhole_attack.inc"/' routing.cc

# Verify the change
grep "wormhole_attack.inc" routing.cc

# Go back to NS-3 root
cd ~/Downloads/ns-allinone-3.35/ns-3.35

# Clean and rebuild
./waf clean
./waf build

═══════════════════════════════════════════════════════════════════════════════

EXPECTED OUTPUT:
════════════════

[2539/2539] Linking build/scratch/routing
'build' finished successfully (XX.XXs)

═══════════════════════════════════════════════════════════════════════════════

AFTER SUCCESSFUL BUILD, TEST THE SIMULATION:
═════════════════════════════════════════════

./waf --run "routing --use_enhanced_wormhole=true --simTime=10"

Expected Files Created:
  ✅ wormhole-attack-results.csv
  ✅ VanetRoutingCompare.csv

═══════════════════════════════════════════════════════════════════════════════
